<h1>Evento <%= @event.name%> do Buffet <%= @event.buffet.brand_name%>  </h1>


<h3><%= @event.description%> </h3>
<div> 
  <strong>Quantidade de convidados: </strong><%= @event.min_guests%><br>
  <strong>Duração: </strong><%= @event.duration%> min<br>
  <strong>Cardápio: </strong><%= @event.menu%><br>
  <strong>Bebidas alcoólicas: </strong><%= @event.alcoholic_beverages ? 'Sim' : 'Não' %><br> 
  <strong>Decoração: </strong><%= @event.decoration ? 'Sim' : 'Não' %><br> 
  
  <strong>Exclusivo no endereço do buffet: </strong><%= @event.event_location ? 'Sim' : 'Não' %><br> 

  <strong>Estacionamento: </strong><%= @event.decoration ? ' com serviço valet' : 'sem serviço valet' %><br>
</div>

<div>
  <strong>  <%= t('activerecord.attributes.price.price_base_weekdays') %>:
  </strong><%= number_to_currency @event.price.price_base_weekdays%><br>

  <strong>  <%= t('activerecord.attributes.price.price_add_weekdays') %>:
  </strong><%= number_to_currency @event.price.price_add_weekdays%><br>

  <strong>  <%= t('activerecord.attributes.price.price_overtime_weekdays') %>:
  </strong><%= number_to_currency @event.price.price_overtime_weekdays%><br>


  <strong>  <%= t('activerecord.attributes.price.price_base_weekend') %>:
  </strong><%= number_to_currency @event.price.price_base_weekend%><br>

  <strong>  <%= t('activerecord.attributes.price.price_add_weekend') %>:
  </strong><%= number_to_currency @event.price.price_add_weekend%><br>

  <strong>  <%= t('activerecord.attributes.price.price_overtime_weekend') %>:
  </strong><%= number_to_currency @event.price.price_overtime_weekend%><br>
</div>


<div>
  <%if current_user_owner == @buffet.user_owner %>
    <%= link_to 'Editar Evento', edit_buffet_event_path( @buffet, @event)%>  
  <%end%>

  <%if user_client_signed_in?%>
    <%= link_to 'Fazer pedido',   new_buffet_event_order_path( @buffet, @event) %>  
  <%end%>

  <%= link_to 'Voltar', buffet_path(@buffet.id)%>  

</div>

  